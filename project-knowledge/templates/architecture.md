# Architecture

> Mark each bullet FACT / INFERENCE / UNCERTAINTY. Never fabricate.

- Style (INFERENCE): (agent) e.g. layered, component SPA, CLI, solution+projects.
- Key areas: (agent) link each to a real directory.
- Data flow: see data-flow.md.
- Open questions (UNCERTAINTY): (agent) list what still needs verification.
